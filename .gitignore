# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Env
.env

# Downloaded/cached datasets — large binary data, not source
training/data/

# Downloaded pretrained model weights — large binary data, not source
*.pt

# Trained router model artifacts — build output, not source
*.joblib
*.pkl
